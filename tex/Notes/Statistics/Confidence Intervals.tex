\chapter{Confidence Intervals}

\section{Definition}

So far, we have seen how we can estimate an unknown population parameter from a random sample. For instance, if the parameter we seek to estimate is the mean $\m$, we can use the sample mean $\ol{x}$ to get estimate a value for $\m$. This is what we call a \vocab{point estimate}. However, a point estimate does not provide any information about the uncertainty present. To this end, it is more desirable to obtain an interval estimate.

\begin{definition}
    An \vocab{interval estimate} of an unknown population parameter is a random interval constructed so that it has a given probability of including the parameter.
\end{definition}

This leads us to the notion of a confidence interval.

\begin{definition}
    Given a fixed value $\a \in [0, 1]$ (known as the \vocab{level of significance}), a \vocab{$100(1 - \a)$\% confidence interval} for an unknown population parameter $\t$ is any interval $(a, b)$ such that $\P{a < \t < b} = 1 - \a$.
\end{definition}

As an example, let us take $\a = 0.05$. If we can find a method of calculating the limits $a$ and $b$, this means that in the long run, if we repeatedly take samples, then the calculated interval $(a, b)$ will contain the population parameter $\t$ for 95\% of the samples taken. Equivalently, the probability of obtaining a random sample for which the corresponding interval contains $\t$ is $0.95$.

Note however, that for a particular sample, we do not know whether this is one of the samples for which $\t$ is in the sample. Our ``confidence'' in the interval comes from the fact that we are using a formula which gives a correct result \emph{most of the time}.

\begin{figure}[H]\tikzsetnextfilename{547}
    \centering
    \begin{tikzpicture}[xscale=0.13, yscale=0.7]
        \def\npoints{100}
        \def\dy{1.1}
        \pgfmathsetmacro{\xmax}{\npoints+2}

        \draw[dashed] (0,30) -- (\xmax,30);
        \foreach \y in {27,28,29,31,32,33}{
            \draw[gray!30, dashed] (0,\y) -- (\xmax,\y);
        }

        \draw (\xmax, 30) -- (\xmax+0.08, 30) node[right] {\small $\m$};

        \foreach \x in {1,...,\npoints}{
            \pgfmathsetmacro{\u}{rand}
            \pgfmathsetmacro{\v}{rand}

            \pgfmathparse{\u < 0.9}
            \ifnum\pgfmathresult=1
                \pgfmathsetmacro{\y}{rand*\dy}
            \else
                \pgfmathparse{\v < 0}
                \ifnum\pgfmathresult=1
                    \pgfmathsetmacro{\y}{\dy+0.5*rand+0.5}
                \else
                    \pgfmathsetmacro{\y}{-\dy-0.5*rand-0.5}
                \fi
            \fi

            \pgfmathparse{ (\y > \dy) || (\y < -\dy) }
            \ifnum\pgfmathresult=1
                \draw[red] (\x,30+\y-\dy) -- (\x,30+\y+\dy);
                \fill[red] (\x,30+\y) circle (0.1);
            \else
                \draw[blue] (\x,30+\y-\dy) -- (\x,30+\y+\dy);
                \fill[blue] (\x,30+\y) circle (0.1);
            \fi
        }

        \end{tikzpicture}
    \caption{95\% confidence intervals for $\m$ computed from 100 different samples. Confidence intervals coloured red do not contain $\m$.}
\end{figure}

\section{Population Mean}\label{S:Confidence-Intervals-Mean}

In this section, we explore interval estimates for the population mean $\m$.

Recall that for a significance level of $\a$, we wish to find an interval $(a, b)$ such that $\P{a < \m < b} = 1 - \a$. To make our lives easier, we impose the condition that the confidence interval be symmetric about $\m$, that is, the interval should be of the form $(\m - E, \m + E)$, where $E$ is the \vocab{margin of error}. However, we obviously do not know $\m$, so we replace $\m$ with its unbiased estimator $\ol{x}$ to get an interval of the form $\bp{\ol{x} - E, \, \ol{x} + E}$. Our goal is thus to find the value of $E$ such that \[\P{\ol{x} - E < \m < \ol{x} + E} = 1 - \a. \tag{$\ast$}\] Depending on the distribution of $\ol{x}$, the margin of error $E$ will differ accordingly.

There are four cases we will consider, with their respectively subsection numbers labelled in the table below:

\begin{table}[H]
    \centering
    {\renewcommand{\arraystretch}{1.2}
    \begin{tabular}{|c|c|cc|}
    \hline
    \multirow{2}{*}{$\s^2$} & \multirow{2}{*}{$n$} & \multicolumn{2}{c|}{Population Distribution} \\ \cline{3-4} 
    &  & \multicolumn{1}{c|}{Normal} & Unknown \\ \hline
    \multirow{2}{*}{Known} & Large & \multicolumn{1}{c|}{\multirow{2}{*}{\SS\ref{S::CI:1}}} & \SS\ref{S::CI:2} \\ \cline{2-2} \cline{4-4} 
    & Small & \multicolumn{1}{c|}{} & \cellcolor{black} \\ \hline
    \multirow{2}{*}{Unknown} & Large & \multicolumn{2}{c|}{\SS\ref{S::CI:3}} \\ \cline{2-4} 
    & Small & \multicolumn{1}{c|}{\SS\ref{S::CI:4}} & \cellcolor{black} \\ \hline
    \end{tabular}}
    \caption{}
\end{table}

\subsection{Normally Distributed Population with Known Variance}\label{S::CI:1}

Suppose our population is normally distributed with unknown mean $\m$ and known variance $\s^2$, so $X \sim \Normal{\m}{\s^2}$. In the previous chapter, we learnt that \[\ol{X} \sim \Normal{\m}{\frac{\s^2}{n}},\] where $n$ is the sample size. Standardizing this, we get \[Z = \frac{\ol{X} - \m}{\s / \sqrt{n}},\] where $Z$ is the standard normal distribution $\Normal{0}{1}$. Manipulating ($\ast$) yields \[\P{\ol{x} - E < \m < \ol{x} + E} = \P{-\frac{E}{\s/\sqrt{n}} < \frac{\ol{x} - \m}{\s/\sqrt{n}} < \frac{E}{\s/\sqrt{n}}} = 1 - \a.\] But we recognize the middle expression as $Z$, so we require \[\P{-\frac{E}{\s/\sqrt{n}} < Z < \frac{E}{\s/\sqrt{n}}} = 1 - \a.\] Because $Z$ is symmetric about 0, we can simplify the above condition to \[\P{Z < \frac{E}{\s/\sqrt{n}}} = 1 - \frac{\a}{2}.\] In order to isolate $E$, we need a way to take the ``inverse'' of a probability of a standard normal distribution. To this end, we introduce the following notation.

\begin{definition}
    Given a probability $c \in [0, 1]$, its \vocab{critical value} $z_c$ is the unique value satisfying $\P{Z < z_c} = c$.
\end{definition}

With this notation, we find that our margin of error is given by \[E = z_{1 - \a/2} \frac{\s}{\sqrt{n}}.\] We thus obtain the following result:

\begin{proposition}
    If $X$ is normally distributed and has known variance $\s^2$, then the symmetric $100(1-\a)$\% confidence interval for $\m$ is given by \[\bp{\ol{x} - z_{1 - \a/2} \frac{\s}{\sqrt{n}}, \, \ol{x} + z_{1 - \a/2} \frac{\s}{\sqrt{n}}}.\]
\end{proposition}

The two limiting values that define the interval are known as the \vocab{$100(1-\a)$\% lower and upper confidence limits}.

Graphically, the area under the probability density function of $\Normal{\ol{x}}{\s^2}$ over the confidence interval is $1 - \a$:

\begin{figure}[H]\tikzsetnextfilename{475}
    \centering
    \begin{tikzpicture}[trim axis left, trim axis right]
        \begin{axis}[
            domain = 0.5:3.5,
            xmin = 0.5,
            xmax = 3.5,
            ymax = 1,
            ymin = -0.1,
            restrict y to domain =-10:5,
            samples = 101,
            axis x line=middle,
            x axis line style={opacity=0},
            xlabel=\empty,
            xtick = {2, 1.168, 2.832},
            xticklabels = {$\ol{x}$, $\ol{x} - z_{1 - \a/2} \frac{\s}{\sqrt{n}}$, $\ol{x} + z_{1 - \a/2} \frac{\s}{\sqrt{n}}$},
            y axis line style={opacity=0},
            ytick=\empty,
            legend cell align={left},
            legend pos=outer north east,
            ]
            
            \addplot[thick] {0};
            \addplot[pattern=north east lines, domain=1.168:2.832] {gauss(2,0.4243)} \closedcycle;
            \addplot[black, very thick] {gauss(2, 0.4243)};
            \draw[dashed] (2, 0) -- (2, 0.940);
        \end{axis}
    \end{tikzpicture}
    \caption{}
\end{figure}

\begin{sample}
    After a rainy night, 12 worms surfaced on the lawn. Their lengths, measured in cm, were: \[9.5, \quad 9.5, \quad 11.2, \quad 10.6, \quad 9.9, \quad 11.1, \quad 10.9, \quad 9.8, \quad 10.1, \quad 10.2, \quad 10.9, \quad 11.0.\] Assuming that this sample came from a normal population with variance 4, calculate a 99\% confidence interval for the mean length of all worms in the garden.
\end{sample}
\begin{sampans}
    Let $X$ cm be the length of a worm. We have $\s = 2$ and $n = 12$. From the sample, we calculate $\ol{x} = 10.392$. Taking $\a = 0.01$, we see that a 99\% confidence interval for the mean length of all worms in the garden is \[\bp{10.392 - z_{0.995} \frac{2}{\sqrt{12}}, 10.392 + z_{0.995} \frac{2}{\sqrt{12}}} = (8.90, 11.9).\]
\end{sampans}

\subsection{Large Sample Size from Any Population with Known Variance}\label{S::CI:2}

In the case where the sample size is large ($n \geq 30$), we can invoke the Central Limit Theorem, regardless of the distribution of the population. If $X$ has variance $\s^2$, then we know from the previous chapter that \[\ol{X} \sim \Normal{\m}{\frac{\s^2}{n}} \text{ approximately}.\] By a similar argument as in \SS\ref{S::CI:1}, we obtain the following (more general) result:

\begin{proposition}
    If $X$ has known variance $\s^2$ and the sample size is large ($n \geq 30$), then the symmetric $100(1-\a)$\% confidence interval for $\m$ is given by \[\bp{\ol{x} - z_{1 - \a/2} \frac{\s}{\sqrt{n}}, \, \ol{x} + z_{1 - \a/2} \frac{\s}{\sqrt{n}}} \text{ approximately}.\]
\end{proposition}

\subsection{Large Sample Size from Any Population with Unknown Variance}\label{S::CI:3}

In most practical situations, it is likely that both the mean and variance are unknown. Provided that the sample size is large ($n \geq 30$), by the Central Limit Theorem, we can say that the distribution of $\ol{X}$ is approximately normal. In place of the unknown population variance $\s^2$, we use $s^2$, the unbiased estimate of the population variance as an approximation. Hence, \[\ol{X} \sim \Normal{\m}{\frac{s^2}{n}} \text{ approximately}.\] Just like before, we get the following result:

\begin{proposition}
    If $X$ has unknown variance, but the sample size is large ($n \geq 30$), then the symmetric $100(1-\a)$\% confidence interval for $\m$ is given by \[\bp{\ol{x} - z_{1 - \a/2} \frac{s}{\sqrt{n}}, \, \ol{x} + z_{1 - \a/2} \frac{s}{\sqrt{n}}} \text{ approximately}.\]
\end{proposition}

\subsection{The $t$-distribution and Degrees of Freedom}

Before looking at confidence intervals of $\m$ when the sample size is small, we first introduce the Student's $t$-distribution.

The crucial statistic in the construction of a confidence interval for the mean of a normal distribution is $Z$, given by \[Z = \frac{\ol{X} - \m}{\s/\sqrt{n}}.\] In \SS\ref{S::CI:3}, when $\s$ was unknown, we were able to substitute the $\s$ with its unbiased estimator $s$ by virtue of the large sample size, which allowed us to approximate $\ol{X}$ with a normal distribution.

However, in the present case where the sample size is small, we do not have such a luxury. More precisely, the test statistic \[T = \frac{\ol{X} - \m}{S / \sqrt{n}}\] is no longer well-approximated by a normal distribution. Here, $T$ depends on two random variables: namely $\ol{X}$ and $S$ (the random variable corresponding to $s$).

\begin{definition}
    The \vocab{Student's $t$-distribution} with $n-1$ degrees of freedom is defined as the distribution of the test statistic \[T = \frac{\ol{X} - \m}{S / \sqrt{n}},\] where $X_i$ are independent and identical normal distributions. We write $T \sim \StudentT{n - 1}$.
\end{definition}

\begin{figure}[H]\tikzsetnextfilename{476}
    \centering
    \begin{tikzpicture}[trim axis left, trim axis right]
        \begin{axis}[
            domain=-2.5:2.5,
            samples=101,
            xmin = -2.5,
            xmax = 2.5,
            ymax = 0.45,
            ymin = -0.1,
            samples = 101,
            axis x line=middle,
            xtick = {0},
            x axis line style={opacity=0},
            y axis line style={opacity=0},
            xlabel = \empty,
            ytick=\empty,
            legend cell align={left},
            legend pos=outer north east,
            ]
            
            \addplot[thick] {0};
            \addplot[red!50, very thick] {1/(pi*(1 + x^2))};
            \addplot[orange!50, very thick] {1/(x^2 + 2)^(3/2)};
            \addplot[yellow!50, very thick] {(8*sqrt(5)/(15*pi))*(1 + x^2/5)^(-3)};
            \addplot[green!50, very thick] {(63*sqrt(10)/512)*(1 + x^2/10)^(-11/2)};
            \addplot[blue!50, very thick] {(9694845*sqrt(30)/134217728)*(1 + x^2/30)^(-31/2)};

            \addplot[black, very thick, dashed] {gauss(0, 1)};
            \draw[dashed] (0, 0) -- (0, 0.399);

            \node[red, anchor=north west] at (-1, 0.158) {$n = 1$};
            \node[blue, anchor=south west] at (0.5, 0.35) {$n = 30$};
            \node[black, anchor=south east] at (-0.5, 0.35) {$Z$};
        \end{axis}
    \end{tikzpicture}
    \caption{The pdfs of $\StudentT{n}$ for $n = 1$, $2$, $5$, $10$, and $30$.}
\end{figure}

From the figure above, we observe that all $t$-distributions are symmetric about 0. Also, as $n \to \infty$, the corresponding $\StudentT{n}$ distribution approaches the standard normal distribution $Z$. In fact, when $n \geq 30$, the difference between the two distributions is negligible, which explains why the normal distribution could be used for cases where $n$ was large in \SS\ref{S::CI:3}.

\subsubsection{Degrees of Freedom}

\begin{definition}
    The \vocab{degrees of freedom} of a statistic is the number of values that are free to vary in its computation.
\end{definition}

Informally, having $n$ degrees of freedom means that once $n$ independent pieces of information are specified, the value of the statistic is completely determined.

As an example, consider our test statistic $T$. We begin with $n$ observations $X_1, \dots, X_n$, which gives us $n$ free values. However, the computation of $T$ requires us to estimate both the sample mean $\ol{X}$ and the unbiased estimator of the population variance $S^2$, which do not vary independently. Indeed, \[S^2 = \frac1{n-1} \sum_{i = 1}^n \bp{X_i - \ol{X}}^2.\] Hence, to compute $S^2$, we must first determine $\ol{X}$, which effectively fixes $\ol{X}$. However, once this happens, the observations $X_1, \dots, X_n$ are no longer independent, as they must now satisfy the constraint \[\ol{X} = \frac{X_1 + \dots + X_n}{n}.\] Since this constraint is linear, it reduces the number of freely varying quantities by one, hence we are left with $n-1$ degrees of freedom. Equivalently, after computing $\ol{X}$, knowing any $n-1$ of the observations $X_i$ uniquely determines the remaining observation.

\subsection{Normally Distributed Population with Unknown Variance and Small Sample Size}\label{S::CI:4}

Suppose $X$ is normally distributed with mean $\m$ and unknown variance. Then \[T = \frac{\ol{X} - \m}{S/\sqrt{n}} \sim \StudentT{n - 1},\] where $S$ is estimated by $s$. Once again, employing a similar argument as in \SS\ref{S::CI:1}, we obtain the following result:

\begin{proposition}
    If $X$ is normally distributed with unknown variance, then the symmetric $100(1-\a)$\% confidence interval for $\m$ is given by \[\bp{\ol{x} - t_{1 - \a/2} \frac{s}{\sqrt{n}}, \, \ol{x} + t_{1 - \a/2} \frac{s}{\sqrt{n}}}.\]
\end{proposition}

Here $t_c$ is the critical value for the $t$-distribution and is given by $\P{T < t_c} = c$.

\subsection{Summary}

The following table shows the appropriate margin of error to be used in different scenarios when finding confidence intervals for the population mean. For conciseness, we use $c = 1 - \a/2$. Cells with gray backgrounds indicate an approximation.

\begin{table}[H]
    \centering
    \bgroup
    \def\arraystretch{1.2}
    \begin{tabular}{|c|c|cc|}
    \hline
    \multirow{2}{*}{$\s^2$} & \multirow{2}{*}{$n$} & \multicolumn{2}{c|}{Population Distribution} \\ \cline{3-4} 
    &  & \multicolumn{1}{c|}{Normal} & Unknown \\ \hline
    \multirow{2}{*}{Known} & Large & \multicolumn{1}{c|}{\multirow{2}{*}{$z_c \s/\sqrt{n}$}} & \cellcolor{black!20} $z_c \s/\sqrt{n}$ \\ \cline{2-2} \cline{4-4} 
    & Small & \multicolumn{1}{c|}{} & \cellcolor{black} \\ \hline
    \multirow{2}{*}{Unknown} & Large & \multicolumn{2}{c|}{\cellcolor{black!20} $z_c s/\sqrt{n}$} \\ \cline{2-4} 
    & Small & \multicolumn{1}{c|}{$t_c s/\sqrt{n}$} & \cellcolor{black} \\ \hline
    \end{tabular}
    \egroup
    \caption{}
\end{table}

\section{Population Parameter}

Suppose we wish to find $p$, the proportion of ``successes'' in a population. For a large sample size $n$, \[P_S \sim \Normal{p}{\frac{p(1-p)}{n}} \text{ approximately},\] where $P_S$ is the sample proportion. Again employing the same argument used in \SS\ref{S::CI:1}, we obtain the following result.

\begin{proposition}
    Given a sample proportion $\hat{p}$, the symmetric $100(1-\a)$\% confidence interval for $p$ is given by \[\bp{\hat{p} - z_{1 - \a/2} \sqrt{\frac{\hat{p}\bp{1 - \hat{p}}}{n}}, \, \hat{p} + z_{1 - \a/2} \sqrt{\frac{\hat{p}\bp{1 - \hat{p}}}{n}}}.\]
\end{proposition}